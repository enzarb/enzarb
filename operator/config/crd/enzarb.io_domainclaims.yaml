---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: domainclaims.enzarb.io
spec:
  group: enzarb.io
  names:
    kind: DomainClaim
    listKind: DomainClaimList
    plural: domainclaims
    shortNames:
    - domainclaim
    singular: domainclaim
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.fqdn
      name: FQDN
      type: string
    - jsonPath: .spec.projectRef
      name: Project
      type: string
    - jsonPath: .status.verifiedAt
      name: Verified
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          DomainClaim is the cluster-scoped ownership ledger for custom domains;
          its name is a hash of the FQDN, so etcd name uniqueness binds a hostname
          to exactly one project.
        properties:
          apiVersion:
            type: string
          kind:
            type: string
          metadata:
            type: object
          spec:
            properties:
              fqdn:
                type: string
              namespace:
                type: string
              orgID:
                type: string
              projectRef:
                type: string
            required:
            - fqdn
            - namespace
            - orgID
            - projectRef
            type: object
          status:
            properties:
              verifiedAt:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
