version: "2"

formatters:
  enable:
    - gofmt
    - goimports

linters:
  enable:
    - govet
    - staticcheck
    - gosec
    - misspell
    - revive
    - unconvert
    - unused
    - errcheck
  settings:
    gosec:
      excludes:
        - G304
    revive:
      rules:
        - name: exported
          disabled: true

run:
  timeout: 5m
